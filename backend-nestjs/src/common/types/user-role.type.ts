/**
 * UserRole type definition
 * Matches Prisma enum UserRole { CUSTOMER, EMPLOYEE, ADMIN }
 * 
 * Once Prisma migrations are applied and client regenerated,
 * this can be replaced with: import type { UserRole } from '@prisma/client'
 */
export type UserRole = 'CUSTOMER' | 'EMPLOYEE' | 'ADMIN';
